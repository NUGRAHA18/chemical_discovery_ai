# Root .env untuk docker-compose
# Copy ke .env dan sesuaikan nilainya

# MongoDB (hanya untuk docker-compose.yml development)
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=changeme

# Redis password (opsional, kosongkan jika tidak dipakai)
REDIS_PASSWORD=
